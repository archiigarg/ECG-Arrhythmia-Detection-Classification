version: "3.9"

services:
  plumber:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"

  flask:
    build:
      context: .
      dockerfile: Dockerfile.flask
    ports:
      - "5001:5000"
    env_file:
      - .env
    depends_on:
      - plumber
